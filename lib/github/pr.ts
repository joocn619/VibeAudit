export async function openFixPullRequest(repoId: string, findingId: string, fixCode: string) {
  // Will implement automated PR opening in Phase 4
  return { prUrl: `https://github.com/example/${repoId}/pull/1`, status: "open" };
}
